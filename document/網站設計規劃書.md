# 社團法人中華亞太文化交流協會 — 網站設計規劃書

> **版本**：v1.1（2026-02-23 更新）
> **部署目標**：GitHub Pages + 自訂網域（`cap-cca.org`）
> **技術限制**：純靜態（HTML / CSS / JavaScript），無伺服器後端，不含會員資料庫

---

## 目錄

1. [專案概述](#1-專案概述)
2. [網站架構（Sitemap）](#2-網站架構sitemap)
3. [檔案結構（File Structure）](#3-檔案結構file-structure)
4. [HTML 頁面模板規範](#4-html-頁面模板規範)
5. [內容維護方案（輕量 CMS）](#5-內容維護方案輕量-cms)
6. [主體配色系統](#6-主體配色系統)
7. [排版與字型規範](#7-排版與字型規範)
8. [UI 元件設計規範](#8-ui-元件設計規範)
9. [響應式設計方針](#9-響應式設計方針)
10. [頁面互動設計](#10-頁面互動設計)
11. [個別頁面規劃](#11-個別頁面規劃)
12. [實作流程](#12-實作流程)
13. [GitHub Pages 部署方式](#13-github-pages-部署方式)

---

## 1. 專案概述

### 1.1 組織簡介

| 項目 | 內容 |
|------|------|
| 名稱 | 社團法人中華亞太文化交流協會 |
| 英文名稱 | Chinese Asia Pacific Cultural Association（CAPCCA） |
| 宗旨 | 促進亞太地區人民間經濟、人文、社會文化之間交流 |
| 性質 | 依人民團體法設立，非以營利為目的之社會團體 |
| 期刊 | 《亞太社科論叢》（每年 4 月、10 月出刊） |

### 1.2 目標受眾

- 對亞太研究有興趣的**學術人員與研究者**
- 尋求企業培訓課程的**企業主管與 HR**
- 有意投稿或訂閱期刊的**學者與學生**
- 欲了解或加入協會的**潛在會員**

### 1.3 設計核心理念

**專業、國際化、學術氣息** — 以簡潔大方的學術機構風格為基準，融入亞太文化的溫暖色調，透過細膩的動態效果提升使用體驗。

### 1.4 架構原則

- **URL 乾淨**：HTML 頁面自 `assets/` 移出至根目錄下的對應資料夾（例如 `about/`、`training/`），避免網址出現 `/assets/` 導致不美觀。
- **內容解耦與多語系準備**：頁面的文字內容儲存於 `assets/Data/` 作為多語系（未來可擴充不同語言 json）的基礎，透過 JS 動態載入。

---

## 2. 網站架構（Sitemap）

以下為使用者看到的 **URL 路徑** 與實體 HTML 檔案對應結構：

```
cap-cca.org/                              ← 首頁
cap-cca.org/about/                        ← 關於協會
cap-cca.org/about/mission/                ← 本會宗旨
cap-cca.org/about/charter/                ← 本會章程
cap-cca.org/training/                     ← 培訓訊息總覽
cap-cca.org/training/courses/             ← 課程列表（六大類目）
cap-cca.org/training/testimonials/        ← 學習心得分享
cap-cca.org/training/register/            ← 外訓報名
cap-cca.org/training/corporate/           ← 企業包班洽詢
cap-cca.org/journal/                      ← 期刊介紹（亞太社科論叢）
cap-cca.org/journal/submission/           ← 投稿規格與稿約
cap-cca.org/journal/issues/               ← 歷期期刊目錄
cap-cca.org/news/                         ← 最新消息列表
cap-cca.org/news/article/                 ← 單篇消息（動態載入）
cap-cca.org/member/                       ← 會員專區（佔位說明頁）
```

### 導覽列一級選單

```
協會首頁 | 關於協會 ▾ | 培訓訊息 ▾ | 學術期刊 ▾ | 最新消息 | 會員專區
```

---

## 3. 檔案結構（File Structure）

```
cap-cca.org/                              ← Repository 根目錄
│
├── index.html                            ← 唯一留在根目錄的 HTML（首頁）
├── cms.html                              ← ⭐ 內容管理工具（多語言文本編輯），忽略於 Git 之外
├── 404.html                              ← 自訂 404 頁面
├── CNAME                                 ← GitHub Pages 自訂網域設定
│
├── about/                                ← 頁面 HTML 自 Assets 移至根目錄
│   ├── index.html                        ← 關於協會概覽
│   ├── mission.html                      ← 本會宗旨
│   └── charter.html                      ← 本會章程
├── training/
│   ├── index.html                        ← 培訓訊息總覽
│   ├── courses.html                      ← 課程列表（Tab 切換六大類）
│   ├── testimonials.html                 ← 學習心得分享
│   ├── register.html                     ← 外訓課程報名
│   └── corporate.html                    ← 企業包班洽詢
├── journal/
│   ├── index.html                        ← 期刊介紹
│   ├── submission.html                   ← 投稿規格
│   └── issues.html                       ← 歷期目錄
├── news/
│   ├── index.html                        ← 消息列表
│   └── article.html                      ← 單篇消息（讀 JSON 動態渲染）
├── member/
│   └── index.html                        ← 會員專區佔位頁
│
├── assets/
│   │
│   ├── Css/
│   │   ├── variables.css                 ← 設計 Token（色彩、字型、間距、動畫速度）
│   │   ├── reset.css                     ← CSS Reset / Normalize
│   │   ├── base.css                      ← 全域基礎樣式（body、a、heading）
│   │   ├── layout.css                    ← 導覽列、頁尾、容器、Grid 系統
│   │   ├── components.css                ← 共用元件（按鈕、卡片、badge、accordion）
│   │   └── animations.css                ← 動畫與過渡效果（fade-in、stagger）
│   │
│   ├── Js/
│   │   ├── main.js                       ← 全域（navbar、scroll、back-to-top、模板注入）
│   │   ├── animations.js                 ← IntersectionObserver 動畫觸發
│   │   ├── components.js                 ← Tab 切換、Accordion、計數器等元件
│   │   └── content-loader.js             ← 從 data/ 讀取 JSON 並渲染內容
│   │
│   ├── Data/                             ← 存放所有文字內容基準，支援多語言
│   │   ├── site-config.json              ← 全站設定（協會名稱、聯絡資訊、社群連結）
│   │   ├── news.json                     ← 最新消息文章列表
│   │   ├── courses.json                  ← 課程資料
│   │   ├── testimonials.json             ← 學習心得
│   │   ├── journal-issues.json           ← 歷期期刊目錄
│   │   ├── zh-TW/                        ← 繁體中文語系（由 cms.html 編輯器產出）
│   │   │   ├── index.json                ← 首頁文字
│   │   │   └── about.json                ← 關於我們文字等...
│   │   └── en/                           ← 英文語系（未來擴充）
│   │
│   ├── Templates/                        ← HTML 片段模板（由 JS 注入）
│   │   ├── navbar.html                   ← 統一導覽列模板
│   │   └── footer.html                   ← 統一頁尾模板
│   │
│   └── Images/
│       ├── logo.svg                      ← 協會 Logo
│       ├── hero-bg.svg                   ← Hero 裝飾 SVG
│       ├── journal-cover.jpg             ← 期刊封面
│       ├── og-image.jpg                  ← Open Graph 分享圖
│       └── news/                         ← 消息文章配圖
│
└── Document/                             ← 規劃文件（不部署到網站）
    ├── 網頁架構.md
    └── 網站設計規劃書.md
```

> **目錄原則**：確保使用者看到的網址以 `/about/`、`/training/` 等呈現，將原本存放於 `assets/pages/` 的 HTML 外移至根目錄。對於頁面內的文字內容，利用 `assets/data/` 的多語系 JSON 檔案，以動態注入方式處理。

---

## 4. HTML 頁面模板規範

### 4.1 設計理念

為確保每個頁面風格完全一致，導覽列與頁尾採用**JavaScript 注入模板片段**的方式實作：

- 導覽列（`assets/Templates/navbar.html`）與頁尾（`assets/Templates/footer.html`）各自是獨立的 HTML 片段
- `main.js` 在頁面載入時使用 `fetch()` 取得這兩個片段，注入到每個頁面的對應 `<div>` 佔位容器
- 日後需要修改導覽列選單或頁尾資訊，**只需修改一個檔案**，所有頁面同步更新

### 4.2 基礎頁面骨架

所有 HTML 頁面（含 `index.html` 與 `assets/pages/**/*.html`）均採用以下骨架：

```html
<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- ── SEO ── -->
  <title>【頁面標題】｜社團法人中華亞太文化交流協會</title>
  <meta name="description" content="【頁面描述，120字以內】">
  <link rel="canonical" href="https://cap-cca.org/【路徑】">

  <!-- ── Open Graph（社群分享） ── -->
  <meta property="og:title" content="【頁面標題】">
  <meta property="og:description" content="【描述】">
  <meta property="og:image" content="https://cap-cca.org/assets/Images/og-image.jpg">
  <meta property="og:type" content="website">

  <!-- ── Favicon ── -->
  <link rel="icon" type="image/svg+xml" href="/assets/Images/logo.svg">

  <!-- ── 字型 ── -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+TC:wght@400;600;700&family=Noto+Sans+TC:wght@300;400;500;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">

  <!-- ── 樣式表（順序固定） ── -->
  <link rel="stylesheet" href="/assets/Css/variables.css">
  <link rel="stylesheet" href="/assets/Css/reset.css">
  <link rel="stylesheet" href="/assets/Css/base.css">
  <link rel="stylesheet" href="/assets/Css/layout.css">
  <link rel="stylesheet" href="/assets/Css/components.css">
  <link rel="stylesheet" href="/assets/Css/animations.css">
  <!-- 若有頁面專屬樣式，在此引入 -->
  <!-- <link rel="stylesheet" href="/assets/Css/about.css"> -->
</head>
<body>

  <!-- ① 導覽列佔位容器（由 main.js 注入 navbar.html） -->
  <div id="navbar-placeholder"></div>

  <!-- ② 頁面頂部 Banner（首頁除外，內頁用） -->
  <section class="page-banner" aria-label="頁面標題區">
    <div class="container">
      <!-- 麵包屑導覽 -->
      <nav class="breadcrumb" aria-label="您的位置">
        <a href="/">首頁</a>
        <span aria-hidden="true">›</span>
        <a href="/about/">關於協會</a>
        <span aria-hidden="true">›</span>
        <span class="current" aria-current="page">本會宗旨</span>
      </nav>
      <h1 class="page-banner__title" data-i18n="about.mission.title">本會宗旨</h1>
    </div>
  </section>

  <!-- ③ 主要內容區塊 -->
  <main id="main-content">
    <!-- 頁面專屬內容填入此處 -->
  </main>

  <!-- ④ 頁尾佔位容器（由 main.js 注入 footer.html） -->
  <div id="footer-placeholder"></div>

  <!-- ⑤ 回頂部按鈕 -->
  <button id="back-to-top" class="back-to-top" aria-label="回到頁面頂端">↑</button>

  <!-- ── JavaScript（順序固定） ── -->
  <script src="/assets/Js/main.js"></script>
  <script src="/assets/Js/animations.js"></script>
  <script src="/assets/Js/components.js"></script>
  <!-- 若有頁面專屬腳本，在此引入 -->
  <!-- <script src="/assets/Js/news.js"></script> -->
</body>
</html>
```

### 4.3 內頁 Banner 樣式

每個子頁面頂部有統一的 **Page Banner** 元件：
- 背景：深藍漸層 + 低透明度協會圖騰浮水印
- 包含**麵包屑導覽**（Breadcrumb），讓使用者知道所在位置
- 頁面 `<h1>` 標題以大字白色呈現
- Banner 高度：Desktop 200px，Mobile 140px

### 4.4 路徑規範

| 情境 | 路徑寫法 | 說明 |
|------|---------|------|
| CSS / JS / 圖片 | `/assets/Css/base.css` | 以根目錄 `/` 開頭的絕對路徑 |
| 頁面間連結 | `/about/` | HTML 檔案保留目錄結構放於根目錄，確保不同深度的頁面皆能呈現乾淨網址 |
| 首頁連結 | `/` 或 `/index.html` | 回首頁 |

> 所有路徑一律使用**以 `/` 開頭的絕對路徑**，避免相對路徑在不同深度頁面出錯。

---

## 5. 內容維護方案（輕量 CMS 與多語系工具）

### 5.1 設計哲學

網站部署於 GitHub Pages（純靜態），無法安裝 WordPress 等傳統 CMS。本方案採用「**JSON 驅動內容**」架構：

- **維護者只需修改 `assets/Data/` 中的 JSON 檔案**，不需接觸 HTML、CSS 或 JavaScript
- 網頁 JavaScript 在瀏覽器端讀取 JSON，自動渲染成網頁內容
- JSON 格式簡單直觀，使用任何文字編輯器（記事本、VS Code）皆可編輯

### 5.2 各 JSON 檔案說明

#### `site-config.json` — 全站設定
```json
{
  "siteName": "社團法人中華亞太文化交流協會",
  "siteNameEn": "Chinese Asia Pacific Cultural Association",
  "abbreviation": "CAPCCA",
  "slogan": "促進亞太地區人民間的經濟、人文、社會文化交流",
  "contact": {
    "email": "info@cap-cca.org",
    "phone": "02-XXXX-XXXX",
    "address": "台北市XXX區XXX路XX號"
  },
  "social": {
    "facebook": "https://facebook.com/cap-cca",
    "youtube": "",
    "line": ""
  },
  "copyright": "2024 社團法人中華亞太文化交流協會 版權所有"
}
```

#### `news.json` — 最新消息
```json
{
  "articles": [
    {
      "id": "2026-001",
      "title": "114年度第一次會員大會通知",
      "category": "公告",
      "date": "2026-02-15",
      "summary": "本會訂於民國114年3月20日舉行第一次會員大會，請各會員準時出席。",
      "content": "詳細內容文字……（支援多行）",
      "image": "/assets/Images/news/2026-001.jpg",
      "pinned": true
    },
    {
      "id": "2026-002",
      "title": "《亞太社科論叢》114年4月號徵稿啟事",
      "category": "期刊",
      "date": "2026-01-20",
      "summary": "本刊歡迎針對亞太地區社會、經濟、教育等議題踴躍投稿。",
      "content": "……",
      "image": "",
      "pinned": false
    }
  ]
}
```

**新增消息步驟**（維護者操作）：
1. 在 `articles` 陣列中**複製任一條目**貼到最前面
2. 修改 `id`（流水號）、`title`、`date`、`summary`、`content`
3. 若有配圖，將圖片放入 `/assets/Images/news/` 並填入路徑；無圖片留空 `""`
4. 存檔後上傳（git push）即自動更新

#### `courses.json` — 課程資料
```json
{
  "categories": [
    {
      "id": "management",
      "name": "經營管理",
      "icon": "📊",
      "description": "提升企業管理能力與領導力",
      "courses": [
        {
          "name": "策略管理實務",
          "hours": 18,
          "audience": "中高階主管",
          "fee": "請洽詢",
          "description": "課程說明……"
        }
      ]
    },
    {
      "id": "ai",
      "name": "AI 類",
      "icon": "🤖",
      "description": "掌握人工智慧應用趨勢",
      "courses": []
    }
  ]
}
```

#### `journal-issues.json` — 歷期期刊
```json
{
  "issues": [
    {
      "volume": 5,
      "issue": 1,
      "date": "2026-04",
      "title": "第五卷第一期",
      "coverImage": "/assets/Images/journal/vol5-no1.jpg",
      "downloadUrl": ""
    }
  ]
}
```

#### `testimonials.json` — 學習心得
```json
{
  "testimonials": [
    {
      "name": "王○○",
      "company": "XX股份有限公司",
      "position": "人資經理",
      "course": "策略管理實務",
      "content": "課程內容充實，講師理論與實務兼具……",
      "date": "2025-11"
    }
  ]
}
```

### 5.3 網頁欄位文字內容管理工具（`cms.html`）

配合多語系支援及方便維護者管理頁面內容，我們提供一個本地端運作的**內容管理工具（`cms.html`）**。此工具**無視於 Git**（放置於 `.gitignore` 清單內）。

**操作流程**：
1. **本機開啟 `cms.html`**：維護者打開該工具。
2. **選擇頁面與語言**：利用下拉選單選擇需編輯的頁面項目與語言版本。
3. **編輯文字內容**：在表單欄位上修改對應文字。
4. **按下「完成編輯」並匯出**：工具會產生對應的更新內容，產生覆蓋用 JSON 檔案（例如 `assets/Data/zh-TW/about.json`）。
5. **Git Push 上傳**：本地端檔案更新後，執行 `git push`（或藉由 GitHub Desktop）上傳完成部署。

> 此機制可以確保非技術人員無需面對傳統 JSON 編輯，降低檔案改壞的風險。

### 5.4 一般資料維護操作流程（消息、課程等）

```
更新消息 / 課程 / 期刊
        ↓
使用文字編輯器開啟對應 JSON 檔
        ↓
複製已有條目格式，填入新內容
        ↓
存檔 → 上傳至 GitHub（git push 或 GitHub 網頁介面直接編輯）
        ↓
GitHub Pages 自動部署（約 1-3 分鐘生效）
```

> **GitHub 網頁介面編輯**（免安裝 git）：
> 直接在瀏覽器開啟 GitHub 倉庫 → 找到 JSON 檔 → 點右上角鉛筆圖示 → 修改 → 點「Commit changes」即完成

### 5.5 靜態章程 / 稿約佔位規範

章程、稿約等**不常變動的長文**直接寫在 HTML 檔案中（不使用 JSON），維護者若需更新可在 HTML 中找到對應段落修改文字即可。HTML 中的文字區塊會用清楚的 HTML 註解標示：

```html
<!-- ===== 章程 第一條 ===== -->
<div class="charter-article">
  <h3 data-i18n="charter.article1.title">第一條</h3>
  <p data-i18n="charter.article1.content">本會名稱為「社團法人中華亞太文化交流協會」。</p>
</div>
<!-- ===== /章程 第一條 ===== -->
```

---

## 6. 主體配色系統

### 6.1 設計哲學

以「**亞太藍**」作為主色調，象徵廣闊的海洋與國際視野；以「**暖金**」作為強調色，體現學術榮耀；以「**象牙白**」為背景基底，維持學術機構的清雅氣質。

### 6.2 色彩定義

```css
:root {
  /* ── 主色調（Asia Pacific Blue） ── */
  --color-primary-900: #0B2545;    /* 深海藍 — 頁首背景、重要標題 */
  --color-primary-800: #13395E;    /* 深藍 — 導覽列 hover 背景 */
  --color-primary-600: #1D6FA4;    /* 標準藍 — 主要按鈕、連結、強調邊框 */
  --color-primary-400: #4A9AC4;    /* 中藍 — 次要 tag 標籤 */
  --color-primary-100: #D6EAF5;    /* 淡藍 — 卡片 hover 背景、區塊底色 */

  /* ── 強調色（Academic Gold） ── */
  --color-accent-600:  #B8860B;    /* 深金 — 重要徽章、邊框強調 */
  --color-accent-500:  #D4A017;    /* 標準金 — CTA 按鈕、重點文字 */
  --color-accent-200:  #F5E9BC;    /* 淡金 — 引用框背景、高亮底色 */

  /* ── 中性色（Neutral） ── */
  --color-neutral-900: #1A1A2E;    /* 主要文字 */
  --color-neutral-700: #3D3D5C;    /* 次要文字、副標 */
  --color-neutral-500: #6B6B8A;    /* 輔助說明文字 */
  --color-neutral-300: #C8C8D8;    /* 分隔線、邊框 */
  --color-neutral-100: #F4F4F8;    /* 區塊底色交替 */

  /* ── 基底 ── */
  --color-bg-primary:  #FAFAF7;    /* 象牙白頁面底色 */
  --color-bg-card:     #FFFFFF;    /* 卡片白 */

  /* ── 功能色 ── */
  --color-success: #2E7D52;
  --color-warning: #C07A00;
  --color-error:   #B00020;
}
```

### 6.3 配色使用原則

| 用途 | 顏色 |
|------|------|
| 導覽列背景 | `--color-primary-900` |
| 主要 CTA 按鈕 | `--color-primary-600` |
| 報名 / 強調按鈕 | `--color-accent-500`（金色） |
| 頁面標題 | `--color-neutral-900` |
| 正文 | `--color-neutral-700` |
| 卡片背景 | `--color-bg-card` |
| 交替區塊背景 | `--color-neutral-100` |
| 頁尾背景 | `--color-primary-900` |

---

## 7. 排版與字型規範

### 7.1 字型選擇

```html
<link href="https://fonts.googleapis.com/css2?
  family=Noto+Serif+TC:wght@400;600;700&
  family=Noto+Sans+TC:wght@300;400;500;700&
  family=Inter:wght@400;500;600&
  display=swap" rel="stylesheet">
```

| 用途 | 字型 |
|------|------|
| 大標題、Logo | `Noto Serif TC`（學術感襯線） |
| 正文、說明 | `Noto Sans TC`（易讀無襯線） |
| 數字、英文標籤 | `Inter`（現代無襯線英文） |

### 7.2 文字尺寸系統

```css
:root {
  --text-xs:   0.75rem;   /*  12px */
  --text-sm:   0.875rem;  /*  14px */
  --text-base: 1rem;      /*  16px — 正文基準 */
  --text-lg:   1.125rem;  /*  18px */
  --text-xl:   1.25rem;   /*  20px */
  --text-2xl:  1.5rem;    /*  24px */
  --text-3xl:  1.875rem;  /*  30px */
  --text-4xl:  2.25rem;   /*  36px */
  --text-5xl:  3rem;      /*  48px — Hero 主標 */
}
```

---

## 8. UI 元件設計規範

### 8.1 導覽列（Navbar）

**外觀：**
- 背景：`--color-primary-900`（固定置頂，`position: sticky`）
- 左側：協會 Logo SVG + 中文全名（Noto Serif TC，白色）
- 中央：主導覽連結（白色，hover 底部出現金色底線動畫）
- 右側：「會員登入」按鈕（金色框線）
- **捲動行為**：捲動超過 80px 轉為毛玻璃效果（`backdrop-filter: blur(12px)` + 半透明深藍背景）

**有下拉選單的一級連結**（關於協會、培訓訊息、學術期刊）：
- Hover 展開下拉選單（Desktop）
- Mobile 改為點擊展開子選單（Accordion 式）

**Mobile 漢堡選單：**
- ≤ 768px 時，主連結隱藏，改為右上角漢堡按鈕（☰）
- 點擊後從右側滑入全螢幕選單（`transform: translateX` 動畫）

### 8.2 按鈕（Button）

```css
/* 主要按鈕（藍色） */
.btn-primary {
  background: var(--color-primary-600);
  color: #fff;
  border-radius: 6px;
  padding: 0.625rem 1.5rem;
  font-weight: 500;
  transition: all 0.25s ease;
  box-shadow: 0 2px 8px rgba(29,111,164,0.3);
}
.btn-primary:hover {
  background: var(--color-primary-800);
  transform: translateY(-2px);
  box-shadow: 0 6px 16px rgba(29,111,164,0.4);
}

/* 強調按鈕（金色 CTA） */
.btn-accent {
  background: var(--color-accent-500);
  color: var(--color-primary-900);
  font-weight: 700;
}
.btn-accent:hover {
  background: var(--color-accent-600);
  transform: translateY(-2px);
}

/* 外框按鈕 */
.btn-outline {
  border: 2px solid var(--color-primary-600);
  color: var(--color-primary-600);
  background: transparent;
}
.btn-outline:hover { background: var(--color-primary-100); }
```

**觸控優化**：所有按鈕最小點擊區域 `min-height: 44px; min-width: 44px`（符合 WCAG 觸控目標尺寸）

### 8.3 卡片（Card）

**消息卡片：**
- 白色背景、`border-radius: 12px`、陰影 `box-shadow: 0 2px 12px rgba(0,0,0,0.08)`
- 頂部 4px 彩色色條（依類別不同）
- Hover：陰影加深 + `translateY(-4px)` 上浮
- 內含：類別 badge、日期、標題、摘要（2行截斷）、「閱讀更多 →」

**課程卡片：**
- 頂部帶類別 icon 的彩色 Banner
- 課程名稱、說明、適合對象 tag、費用、「查看課程」按鈕

### 8.4 區塊標題（Section Header）

```html
<div class="section-header">
  <span class="section-label">關於協會</span>    <!-- 小字金色標籤 -->
  <h2 class="section-title">本會宗旨與使命</h2>
  <div class="section-divider"></div              <!-- 藍色短橫線裝飾 -->
</div>
```

### 8.5 頁尾（Footer）

- 背景：`--color-primary-900`
- Desktop：四欄排版（協會簡介 / 快速連結 / 聯絡資訊 / 社群媒體）
- Mobile：單欄堆疊
- 連結 hover 轉金色
- 底部版權聲明（從 `site-config.json` 讀取）

### 8.6 Accordion（折疊元件）

用於章程、稿約條款：
- 點擊標題展開 / 收合（`max-height` transition）
- `+` 圖示旋轉為 `×`

### 8.7 Tab 切換（課程分類）

- 水平 Tab 列，底部有滑動指示條（`left / width` transition）
- 內容 fade-in 淡入，切換流暢

### 8.8 圖片與圖示資源規範（Fallback 機制）

- **既有圖檔優先**：所有圖片與影像設計資源，需優先確認與使用 `/assets/Images/` 目錄下所提供之圖檔。
- **缺圖處理（SVG 優先）**：若在設計開發過程中發現 `/assets/Images/` 中未提供對應或所需之圖片，應**優先以 SVG 向量圖像**作為設計替代方案（取代傳統的 PNG/JPG 佔位圖）。
- **設計考量**：採用 SVG 可確保跨裝置與高解析度螢幕（Retina）下的視覺銳利度，有助於維持學術網站的專業精緻度並優化載入效能。

---

## 9. 響應式設計方針

### 9.1 斷點定義（Mobile First）

```css
/* 預設：手機 (0px+) */
@media (min-width: 576px)  { /* 手機橫向 */ }
@media (min-width: 768px)  { /* 平板 */ }
@media (min-width: 1024px) { /* 小型桌機，導覽列切換 */ }
@media (min-width: 1280px) { /* 標準桌機 */ }
@media (min-width: 1536px) { /* 大螢幕 */ }
```

### 9.2 版面調整對照

| 元件 | Desktop（≥ 1024px） | Mobile（< 768px） |
|------|---------------------|------------------|
| 導覽列 | 水平完整選單 + 下拉 | 漢堡 → 右側滑出 |
| Hero CTA 按鈕 | 並排（flex row） | 上下堆疊（flex column） |
| 消息卡片 | 3 欄 Grid | 單欄列表 |
| 課程卡片 | 3 欄 Grid | 單欄 |
| 關於協會 | 圖文左右排 | 圖片上、文字下 |
| 課程 Tab | 水平 Tab 列 | 水平可捲動 Tab |
| 收合章程 | Accordion + 側邊目錄 | Accordion（側欄隱藏） |
| 期刊目錄 | 3 欄 Grid | 雙欄 Grid |
| 頁尾 | 四欄 | 單欄堆疊 |
| 麵包屑 | 完整顯示 | 僅顯示目前頁名 |

### 9.3 觸控友善原則

- 所有可點擊元素最小 **44 × 44px** 觸控目標
- 間距使用 `clamp()` 在小螢幕自動縮減（避免擁擠）
- 下拉選單在 Mobile 改為點擊觸發（避免 hover 無效）
- 表單輸入欄位字型大小 ≥ 16px（避免 iOS 自動縮放）
- 行動裝置的字型基準調整：`font-size` 在小螢幕降至 15px，避免換行過多

---

## 10. 頁面互動設計

### 10.1 入場動畫（IntersectionObserver）

各區塊在進入視窗時觸發 **Fade-in + Slide-up**：

```javascript
// animations.js
const observer = new IntersectionObserver((entries) => {
  entries.forEach((entry, i) => {
    if (entry.isIntersecting) {
      entry.target.style.transitionDelay = `${i * 0.08}s`;
      entry.target.classList.add('visible');
      observer.unobserve(entry.target);
    }
  });
}, { threshold: 0.12 });

document.querySelectorAll('.animate-on-scroll').forEach(el => observer.observe(el));
```

```css
/* animations.css */
.animate-on-scroll {
  opacity: 0;
  transform: translateY(28px);
  transition: opacity 0.5s ease, transform 0.5s ease;
}
.animate-on-scroll.visible {
  opacity: 1;
  transform: translateY(0);
}
```

### 10.2 導覽列捲動毛玻璃效果

```javascript
window.addEventListener('scroll', () => {
  document.querySelector('.navbar')
    .classList.toggle('scrolled', window.scrollY > 80);
});
```

```css
.navbar.scrolled {
  background: rgba(11, 37, 69, 0.92);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  box-shadow: 0 2px 20px rgba(0,0,0,0.3);
}
```

### 10.3 首頁 Hero 區塊

- 背景：深藍漸層 + 低透明度亞太地圖 SVG 浮水印
- 少量浮動光點（純 CSS animation，無第三方套件）
- 標題：大字白色（Noto Serif TC）
- 副標：英文名稱 + slogan
- CTA：「了解協會」（主要藍色）+「培訓課程」（外框白色）

### 10.4 平滑捲動

```javascript
// 修正導覽列高度遮擋問題
document.querySelectorAll('a[href^="#"]').forEach(a => {
  a.addEventListener('click', e => {
    e.preventDefault();
    const target = document.querySelector(a.getAttribute('href'));
    const offset = document.querySelector('.navbar').offsetHeight + 16;
    window.scrollTo({ top: target.offsetTop - offset, behavior: 'smooth' });
  });
});
```

### 10.5 統計數字計數器（首頁）

```javascript
// 進入視窗後數字從 0 跳動至目標值
function animateCounter(el) {
  const target = parseInt(el.dataset.target);
  const duration = 1800;
  const step = target / (duration / 16);
  let current = 0;
  const timer = setInterval(() => {
    current = Math.min(current + step, target);
    el.textContent = Math.floor(current).toLocaleString();
    if (current >= target) clearInterval(timer);
  }, 16);
}
```

### 10.6 回頂部按鈕

- 捲動超過 300px 顯示（fade-in）
- 點擊 `window.scrollTo({ top: 0, behavior: 'smooth' })`
- 固定於右下角，手機端縮小避免遮擋內容

---

## 11. 個別頁面規劃

### 11.1 首頁（`/index.html`）

```
[Navbar]
[Hero]            深藍背景 + 協會名稱 + slogan + 兩個 CTA
[最新消息]        3 張消息卡片（從 news.json 讀取最新 3 篇）+ 「查看全部」
[關於協會]        宗旨簡介文字 + 協會圖片（左右排版）
[培訓課程]        六大類別 icon 卡（點擊連至 /training/courses.html）
[期刊介紹]        期刊封面 + 簡介 + 「投稿說明」按鈕
[統計數字]        動態計數（會員人數、課程場次、出刊期數…）
[Footer]
```

### 11.2 關於協會（`/assets/pages/about/index.html`）

- Page Banner + 麵包屑
- 宗旨全文（引用框樣式）
- 協會沿革（Timeline 時間軸元件）
- 子頁連結卡（宗旨 / 章程）

### 11.3 本會章程（`/assets/pages/about/charter.html`）

- Accordion 呈現各條款
- 側邊 Sticky 目錄（Desktop），Mobile 隱藏
- PDF 下載按鈕

### 11.4 培訓訊息（`/assets/pages/training/index.html`）

- 總覽 Banner + 六大類別卡片（從 courses.json 讀取）
- 雙 CTA：「個人報名」＋「企業洽詢」

### 11.5 課程列表（`/assets/pages/training/courses.html`）

- 水平 Tab 切換六大類（從 courses.json 動態渲染）
- 每個 Tab 內容：課程卡片列表

### 11.6 期刊介紹（`/assets/pages/journal/index.html`）

- 期刊封面展示 + 簡介
- 出刊時程說明
- CTA：「歷期目錄」＋「投稿規格」

### 11.7 投稿規格（`/assets/pages/journal/submission.html`）

- 稿約全文（HTML 靜態內容 + Accordion 收合次要項目）
- 投稿方式說明

### 11.8 最新消息列表（`/assets/pages/news/index.html`）

- 從 `news.json` 讀取，渲染消息卡片列表
- 篩選按鈕：全部 / 公告 / 期刊 / 活動
- 置頂（`pinned: true`）的文章固定顯示在最上方

### 11.9 單篇消息（`/assets/pages/news/article.html`）

- URL 帶 `?id=2026-001` 參數
- `content-loader.js` 讀取 `news.json`，找到對應 id，渲染文章全文

### 11.10 會員專區（`/assets/pages/member/index.html`）

- 說明功能規劃中
- 提供聯絡資訊

---

## 12. 實作流程

### Phase 1：基礎建設 ✦ 設計系統

- [ ] `assets/Css/variables.css`（Token 定義）
- [ ] `assets/Css/reset.css`
- [ ] `assets/Css/base.css`
- [ ] `assets/Css/layout.css`（Navbar、Footer、Page Banner、Container Grid）
- [ ] `assets/Css/components.css`（Button、Card、Badge、Accordion、Tab）
- [ ] `assets/Css/animations.css`（Keyframe、.animate-on-scroll）
- [ ] `assets/Templates/navbar.html`（統一導覽列模板）
- [ ] `assets/Templates/footer.html`（統一頁尾模板）
- [ ] `assets/Js/main.js`（模板注入、Navbar 捲動、Mobile 選單、回頂部）
- [ ] `assets/Js/animations.js`（IntersectionObserver）
- [ ] `assets/Js/components.js`（Tab、Accordion、Counter）
- [ ] `assets/Js/content-loader.js`（JSON 讀取 → DOM 渲染）

### Phase 2：資料建立 ✦ JSON 內容

- [ ] `assets/Data/site-config.json`
- [ ] `assets/Data/news.json`（示範資料 × 5 篇）
- [ ] `assets/Data/courses.json`（六大類 × 每類 2 門示範課程）
- [ ] `assets/Data/testimonials.json`
- [ ] `assets/Data/journal-issues.json`

### Phase 3：首頁

- [ ] `index.html` 全部區塊（Hero、消息、關於、課程、期刊、統計）

### Phase 4：子頁面

- [ ] `about/index.html`（含 Timeline 元件）
- [ ] `about/mission.html`
- [ ] `about/charter.html`（Accordion）
- [ ] `training/index.html`
- [ ] `training/courses.html`（Tab + JSON 渲染）
- [ ] `training/register.html`
- [ ] `training/corporate.html`
- [ ] `journal/index.html`
- [ ] `journal/submission.html`
- [ ] `journal/issues.html`
- [ ] `news/index.html`（JSON 渲染 + 篩選）
- [ ] `news/article.html`（query param 讀取）
- [ ] `member/index.html`
- [ ] `404.html`

### Phase 5：優化與測試

- [ ] 響應式測試（320px / 375px / 768px / 1280px）
- [ ] 跨瀏覽器（Chrome / Firefox / Safari / Edge）
- [ ] SEO Meta 完整性（每頁 title、description、og:image）
- [ ] 無障礙（ARIA、鍵盤導覽、色彩對比 WCAG AA）
- [ ] 效能（Lighthouse Score ≥ 90）
- [ ] Favicon 設定

---

## 13. GitHub Pages 部署方式

### 13.1 Repository 設定

1. 在 GitHub 組織帳號建立 repository（建議命名 `cap-cca.org` 或 `<org>.github.io`）
2. 推送所有靜態檔案至 `main` 分支根目錄
3. **Settings → Pages** 啟用，來源選 `main` 分支 `/（root）`

### 13.2 自訂網域設定

1. 根目錄建立 `CNAME` 檔：
   ```
   cap-cca.org
   ```
2. DNS 設定（於網域商後台）：
   ```
   A  @  185.199.108.153
   A  @  185.199.109.153
   A  @  185.199.110.153
   A  @  185.199.111.153
   CNAME  www  <org>.github.io
   ```
3. **Settings → Pages** 填入 `cap-cca.org`，勾選「Enforce HTTPS」

### 13.3 注意事項

- 所有路徑使用 **`/` 開頭的絕對路徑**，避免子目錄層級導致資源 404
- JSON 透過 `fetch('/assets/Data/news.json')` 讀取，需透過 HTTP(S) 協定才能運作（本地開發需用 live server，不能直接雙擊 HTML 開啟）
- `content-loader.js` 搭配 URL `?id=` 參數實作單篇文章，不需後端
- 為避免 `fetch` 被瀏覽器快取導致更新後看不到最新內容，JSON 請求加上時間戳：
  ```javascript
  fetch(`/assets/Data/news.json?v=${Date.now()}`)
  ```

---

*文件版本 v1.1 — 持續更新，實作進度請參照 Phase 清單。*
